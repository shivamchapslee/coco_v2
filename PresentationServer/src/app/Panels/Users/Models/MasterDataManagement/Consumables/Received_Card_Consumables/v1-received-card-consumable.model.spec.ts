import { V1ReceivedCardConsumable } from './v1-received-card-consumable.model';

describe('V1ReceivedCardConsumable', () => {
  it('should create an instance', () => {
    expect(new V1ReceivedCardConsumable()).toBeTruthy();
  });
});
