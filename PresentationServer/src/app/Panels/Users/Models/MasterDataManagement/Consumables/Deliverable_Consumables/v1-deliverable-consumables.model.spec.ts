import { V1DeliverableConsumables } from './v1-deliverable-consumables.model';

describe('V1DeliverableConsumables', () => {
  it('should create an instance', () => {
    expect(new V1DeliverableConsumables()).toBeTruthy();
  });
});
