import { V1ReceivedConsumables } from './v1-received-consumables.model';

describe('V1ReceivedConsumables', () => {
  it('should create an instance', () => {
    expect(new V1ReceivedConsumables()).toBeTruthy();
  });
});
